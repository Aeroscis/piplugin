# 贡献约定（Contributing）

这份文档说的是**怎么改这个仓库**：文档放哪里、待办做完之后怎么写、提交时怎么暂存、
什么必须请人验证。代码风格（`.clang-format`）、检查入口（`scripts/verify.ps1`）与
上手步骤（`docs/tutorial/quickstart.md`）不在这里重复。

## 1. 文档分层

| 位置 | 放什么 |
|---|---|
| `docs/design/` | 机制与设计知识：为什么这样做、平台行为、契约。平台知识（X11 的 XID、DWM 的合成时序）算机制 |
| `docs/tutorial/` | 操作步骤：照着做能跑起来 |
| `docs/todo/` | 待办，按主题拆分（`platform.md` / `adapters.md` / `framework.md` / `build.md` / `tests.md`） |
| `docs/README.en.md` | 英文版文档入口 |
| 根 `README.md` / `CHANGELOG.md` | 门面与版本变更 |

判断标准是"读者拿它干什么"：**解释机制**进 `design/`，**照做**进 `tutorial/`，
**还没做**进 `todo/`。

## 2. 待办条目做完之后

不要把通用知识留在 todo 里"就地归档"——todo 是会被划掉的清单，知识会跟着它一起腐烂。

1. 把**通用知识**抽到 `docs/design/` 下的独立文档：跨项目可复用、不绑定这一次的 bug；
2. todo 条目本身**精简为「结论 + 链接」**——标题追加「—— 已完成（出处）」，
   正文保留原文当检查项清单，前面加一段 `> **状态**：` 引用块写结论与验证方式；
3. 同一主题的总览表（如 `docs/todo/README.md`）同步勾掉。

这个分工不只是口头约定：`scripts/verify.ps1` 第 5 项按
`scripts/doc_drift_rules.json` 的「特征 → 禁用陈述」表反查文档，而**标题带
已完成 / 已落地 / 已达成 / 已修复 的章节整段豁免**——保留下来的原文因此不会被
当成"对今天的断言"。写文档时顺着这个约定走，别让 lint 变成对手。
细节与验收方法见 `docs/todo/tests.md` #8。

## 3. 语言分工

- `docs/` 主体**中文**；需要英文门面时另写 `docs/README.en.md`，不在同一篇里混排。
- `CHANGELOG.md` **英文**：读者是"要不要升级"的外部应用，按版本分节，每条写清
  改了什么、对使用者有什么影响。
- 提交信息**英文 + conventional 前缀**（`feat:` / `fix:` / `docs:` / `ci:` /
  `refactor:` / `test:` / `chore:`），正文按"纠正的事实 / 影响 / 验证"分节。

## 4. 提交纪律

- **显式 pathspec**：`git add <具体文件…>`，不用 `git add -A` / `git add .`。
  暂存后看一眼 `git status`，确认没夹带工作区里的临时文件。
- 规划类派工件（发布路线图、并行派工板这类**一次性**文档）**有意不入库**：
  它们记录的是当时的执行计划，入库就会变成需要长期维护的"第二份真相"。
  相应地，入库文档里**也不要写它们的文件名或链接**——新克隆的读者打不开，
  只会拿到死链。
- `.workbuddy/` 之类的工作目录（会话记忆、临时日志、探针产物）已被 `.gitignore`
  排除，不要用 `-f` 破例提交。
- **每条能被核实的断言都要留下复核入口**：数字（断言数、用例数、导出符号数）
  要能从 `ctest` / `scripts/verify.ps1` / 文档里明确写出的命令复现。
  一次性探针的结论可以固化进文档，但要在文档里写清它是怎么测出来的
  （例如 Xlib 探针：`scripts/probe_x11_native_window.sh`）。

## 5. 什么必须请人验证

自动化只覆盖**稳态**：程序化 `SetWindowPos` + 截图能证明"缩放后几何不变"。
"拖动过程中有没有可感知的闪烁"这类**瞬态观感**由人手决定，脚本不该假装覆盖它。
把边界写清楚并请人确认，比补一个测不出问题的用例诚实。
机制与排查清单见 `docs/design/d3d-window-resizing.md`。
